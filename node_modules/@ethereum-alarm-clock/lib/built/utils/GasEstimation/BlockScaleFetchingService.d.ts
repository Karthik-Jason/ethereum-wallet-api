import { IGasPriceFetchingService, BlockScaleInfo } from '../GasPriceUtil';
export declare class BlockScaleFetchingService implements IGasPriceFetchingService {
    private apiAddress;
    fetchGasPrice(): Promise<BlockScaleInfo>;
}
